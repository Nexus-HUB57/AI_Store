name: Test Deploy Debug

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      deploy_target:
        description: 'Test'
        type: choice
        options:
          - all

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - run: echo "step1"
      - env:
          TEST_SECRET: ${{ secrets.CREDENCIAIS_HOSTGATOR }}
        run: |
          echo "secret length: ${#TEST_SECRET}"
          echo "done"
